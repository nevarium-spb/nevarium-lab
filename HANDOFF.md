# Handoff: Невариум ЛАБ ИИ

Документ для того, кто продолжит эту работу без контекста предыдущей сессии. Читать сверху вниз.

## Что это за проект

Сайт-визитка "Невариум ЛАБ ИИ" — лаборатория внедрения ИИ в малый и средний бизнес.
Владелец: ИП Макеева Марина Александровна. Сайт на русском языке.

- **Код**: `G:\nevarium1` (Astro 7 + TypeScript; React — точечно, только для интерактивных
  островов: формы, чат-бот, шапка, живой фон, FAQ-аккордеон)
- **GitHub**: https://github.com/nevarium-spb/nevarium-lab
- **Прод**: https://nevarium1.vercel.app и https://nevarium-lab.ru (тот же деплой, ветка `main`)
- **CI/CD**: `git push origin main` → Vercel сам собирает и выкатывает прод за 1-2 минуты. Ручной `vercel deploy --prod` не нужен, разве что для отладки.
- **Память проекта** (для будущих сессий Claude): `C:\Users\amake\.claude\projects\G--nevarium1\memory\` — там записаны профиль пользователя, техническая база сайта, схема деплоя и накопленные пожелания пользователя по стилю работы. Стоит прочитать перед продолжением.

## ⚠️ Активная проблема прямо сейчас — CRM не задеплоена, а сайт уже в проде

`claude/astro-migration` **уже слит в `main`** (слияние линейное, без merge-коммита — просто
`main` продвинут вперёд) и **уже в проде**: `nevarium-lab.ru` отдаёт актуальный Astro-HTML
(проверено `curl` на этой сессии — 10 услуг, «Десять точек» на `/services`). Пункт 2 бывшего
блокера («владелица сверила preview») снят тем, что теперь это уже не preview, а сам прод.

Но пункт 1 **не снят и активен прямо сейчас**: CRM (`crm-nevarium.ru`) по-прежнему не отвечает
(перепроверено на этой сессии — `curl` не резолвит/не подключается). Форма на «Контактах»
и чат-бот шлют заявки туда через `src/lib/leads.ts`, и раз CRM недоступна — **любой реальный
посетитель сайта сейчас получает честную ошибку вместо успеха при отправке заявки** (это
осознанное поведение — молчаливого фальшивого «успеха» быть не должно, см. `leads.ts`), а не
теоретический риск на будущее. Первым делом в следующей сессии — проверить, не поднялась
ли CRM: `curl https://crm-nevarium.ru` или сессия в `G:\nevarium-crm`. Как именно решили
пушить в прод до готовности CRM — не задокументировано в этой сессии, у пользователя стоит
уточнить, если это не было осознанным решением.

## Что уже сделано (в хронологии)

1. **Редизайн "Индиго Лайв"** — полная смена визуального стиля: тёмная тема по умолчанию + переключаемая светлая, живой canvas-фон с созвездием (реагирует на курсор, останавливается на скрытой вкладке), индиго-палитра, шрифты Poppins/Open Sans, дашборд-панель в hero.
2. **Интеграция Max.ru** — мессенджер Max (не путать с VK Max) добавлен в контакты и в финальное сообщение чат-бота «Нева». Иконка — фирменный градиентный кружок Max.
3. **Контакты и юрреквизиты** — email и телефон выведены как SVG-текст (защита от парсеров-скрейперов), добавлена услуга «Разработка веб-сайтов», футер переписан: слева ИП-реквизиты, по центру copyright на английском, справа «Сделано с ИИ…».
4. **Убрано слово «бесплатно» по всему сайту** — топбар, кнопки, чат-бот, FAQ, CTA-блоки. Это принципиальная позиция владелицы: **она не делает ничего бесплатно**, ни при каких формулировках.
5. **Развёртывание**:
   - Попытка на **SprintHost по FTP провалилась** (ошибка авторизации) — отказались, не возвращаться к этому варианту без явной новой причины.
   - Развернули на **Vercel** (`vercel deploy --prod`), затем подключили **GitHub-репозиторий** через Vercel Dashboard → Project Settings → Git — теперь автодеплой на каждый push.
   - Подключили собственный домен **nevarium-lab.ru** (куплен на Ru-Center/nic.ru): DNS переключены на `ns1.vercel-dns.com` / `ns2.vercel-dns.com`. Статус проверен явно (`vercel domains inspect`) — резолвится без warning'ов, SSL в порядке.
6. **Новый логотип** — знак N вырезан программно из `public/logo.png` (через временно установленный `sharp`, потом удалён):
   - `public/logo-mark.png` — знак N на прозрачном фоне, в шапке. Текст рядом — обычный HTML.
   - `public/favicon.png` — тот же знак со скруглённой тёмной плашкой-фоном.
7. **Кнопка «Бесплатный аудит» → «Обсудить проект»** — совпадает с кнопкой в шапке.
8. **Продающий рерайт всех текстов сайта** — 7 услуг, 6 сценариев, 3 тарифа, 6 FAQ, 4 шага процесса, сценарий чат-бота, мета-теги. Подробности — в разделе «Логика текстов» ниже.
9. **Интеграция с Невариум CRM** — форма на «Контактах» и чат-бот отправляют заявки через `src/lib/leads.ts`. Честная ошибка с запасным каналом связи при недоступной CRM — не фальшивый экран успеха.
10. **Страница политики обработки персональных данных** (`/privacy`, 152-ФЗ) + форма запроса по ПДн (доступ/исправление/удаление/прекращение обработки) с тем же принципом честной ошибки.
11. **Хардинг по итогам независимого аудита и адверсариального ревью Codex**: SPA-fallback (тогда ещё актуально для React-роутинга), security headers, точечное обновление зависимостей, self-hosted шрифты вместо Google Fonts, защита от повторной отправки в чат-боте, `maxLength` на полях форм.
12. **Миграция с React SPA на Astro** (план — `docs/MIGRATION_PLAN.md`, 7 фаз, ветка `claude/astro-migration`):
    - Все 8 страниц теперь отдаются готовым HTML — соцсети и поисковики видят настоящий контент и свою карточку на каждой странице, а не один документ на весь сайт.
    - React остался только в интерактивных островах (формы, чат-бот, шапка, живой фон, FAQ); статичные страницы не грузят React вообще.
    - Добавлены: canonical, Open Graph/Twitter Card со своей картинкой-превью на каждой странице, JSON-LD (`ProfessionalService` + `Service`), `sitemap.xml`, `robots.txt`.
    - `src/legacy/` (старое React-приложение), `react-router-dom` и мёртвые компоненты удалены; SPA-rewrite убран из `vercel.json` — больше не нужен.
    - **Важная находка**: `client:visible` для форм с персональными данными — риск (при задержке гидратации клик уходит в нативную GET-отправку, данные утекают в URL). Обе формы с ПДн переведены на `client:load`.
13. **Мелкие правки поверх завершённой миграции** (та же ветка `claude/astro-migration`):
    - Поле контакта в форме на «Контактах»: «Телефон или Telegram» → «Телефон или Max» (Telegram убран как формулировка — политический контекст; Max и так везде на сайте основной мессенджер).
    - Бегущая строка на главной (`marqueeItems` в `src/pages/index.astro`) дополнена списком актуальных нейросетей по запросу владелицы: Seedance, Sora, Veo, Suno, Stable Audio, DeepSeek, Grok, Claude, ChatGPT, Claude by Anthropic for Excel, Gemini, Perplexity, Qwen, Kimi, YandexGPT, GigaChat — плюс от себя добавлены Flux/Kandinsky/Kling/Luma/Pika (уже названы в тексте услуги «Генерация визуала и видео», но не входили в присланный список). Исходные 8 фраз-услуг сохранены — список дополнен, не заменён (с первой попытки ошибочно заменил целиком, поправлено по замечанию).
    - Скорость бегущей строки (`.marquee__track` в `src/styles/global.css`) снижена по запросу: `animation-duration` 30s → 60s → **120s** (текущее значение).
14. **Три новые услуги** по итогам разбора гайда «30 способов использовать ChatGPT» — владелица подтвердила, что все пять кандидатов из анализа совпадают с её экспертизой, но чтобы не раздувать страницу «Услуги» до 12 карточек, три близких по смыслу пункта объединены в две услуги, а репёрпоузинг вошёл строкой в уже существующую «ИИ-контент и маркетинг» вместо отдельной карточки:
    - **«Воронки продаж и лид-магниты»** (объединяет прогревающие цепочки + лид-магниты)
    - **«Упаковка личного бренда»** (позиционирование эксперта) — совпадает с сегментом «Личный бренд», который уже был в кейсах
    - **«Сценарии для курсов и вебинаров»** — совпадает с сегментом «Онлайн-школы» в кейсах
    - Добавлены 3 новые иконки в `src/data/icons.tsx`: `IconFunnel`, `IconUser`, `IconPlay`
    - Услуг теперь **10**, не 7 — заголовок на `/services.astro` и карта страниц в `docs/DESIGN_DOC.md` обновлены. Если где-то ещё встретится захардкоженное «7 услуг» — это устарело, поправить.
15. **`claude/astro-migration` слита в `main` и задеплоена в прод** (fast-forward, без merge-коммита — `main` просто продвинут до `5e733d6`, тот же коммит, что был верхушкой ветки). Сразу после слияния работа продолжилась прямо на `main`, тем же самым потоком коммитов: `b56c4cb` — это и есть коммит из п.14 (три новые услуги), просто он уже на `main`, а не на отдельной ветке; дальше на `main` легли ещё коммиты:
    - Три новые услуги из п.14 сначала попали только в `src/data/content.tsx` — пропущены на главной (сетка была grid-3, не совпадала карточками с /services), в выпадающем списке `ContactForm.tsx` (захардкожен под старые 7 пунктов) и в сценарии чат-бота `bot.ts` (текстом перечислял старые 7). Все три места дозаполнены отдельным коммитом.
    - Карточки услуг на `/services` переведены с полноширинных строк на сетку **2×5**: описание и список «что входит» внутри карточки теперь друг под другом (не бок о бок — не помещалось бы), кнопка «Обсудить задачу» прижата к низу карточки через `margin-top: auto`.
    - **Чат-бот-баг**: пользователь показал скриншот — вопрос про «разработку обучающей программы» не давал кнопки «← В начало». Причины: (1) узлы `services`/`fallback` не имели этой кнопки в `quick`, добавлено; (2) ключевое слово «разработ» в паттерне `services` матчилось на «разработать» в любом контексте — убрано; (3) после этого фраза зацепилась за `svc_bots`, потому что голое «бот» матчится подстрокой внутри «разработать/работать/заработать» (раз-ра-**бот**-ать) — более широкая находка, могла проявиться на массе обычных глаголов; исправлено отрицательным lookbehind на кириллическую букву перед «бот» в `bot.ts`.
    - Добавлена ссылка на VK-сообщество (`https://vk.ru/nevarium_lab`) — `IconVK` в `icons.tsx`, блок в футере и на «Контактах» по образцу уже существующего Max, плюс VK добавлен в `sameAs` у JSON-LD в `Base.astro`.

Всё перечисленное закоммичено и **запушено в `main`, уже в проде**. Working tree чист.

## Логика текстов (важно не сломать при правках)

Тексты построены по стратегии **SEE-THINK-DO-CARE** с мягким проведением по **лестнице Ханта**, формулировки опираются на **JTBD**. Ключевые решения:

- **УТП по принципу «выбор без выбора»**: «Внедрять ИИ — уже не выбор. Вопрос — как». Смещает решение клиента с «делать или не делать» на «спроектировать самому или догонять». Это спина всех текстов — если менять h1 на главной, менять осознанно.
- **NDA выведен в 6 точек** (первый экран, процесс, тарифы, FAQ, подпись у формы, чат-бот) — это главное возражение аудитории про безопасность данных.
- **Гарантия намеренно смягчена**: не «дорабатываем за свой счёт», а «фиксируем метрику до старта». Владелица попросила не брать жёстких обязательств.
- **FOMO намеренно НЕ используется** (никаких «осталось 3 места», таймеров, дедлайнов) — требование неагрессивности. Двигатель — неизбежность в УТП, а не дефицит.
- **Слово «боль» не используется** — только «ситуация». Это явное требование владелицы.
- **Слово «бесплатно» не используется нигде** — принципиальная позиция (см. грабли №1).

## Честность доказательной базы (критично)

Кейсы, цифры и отзывы на сайте — **демонстрационные, а не реальные**. Владелица это подтвердила и планирует со временем заменить их настоящими. Поэтому в текстах сделано так:

- Блок отзывов на главной **заменён** на «С чем к нам приходят чаще всего»: выдуманные люди с именами и звёздами убраны (это был риск по 38-ФЗ «О рекламе» и по доверию), вместо них — обезличенные ситуации по сегментам. В элементе `.quote__stars` подпись «Ситуация» вместо звёзд.
- Цифры на главной поданы как ориентиры («до 70%», «до +38%») — в Astro-версии те же значения, но теперь ещё и видны поисковикам сразу в HTML (раньше в исходном коде стоял 0, а настоящее число дорисовывал JS).
- В лиде страницы сценариев прямо написано: «это не отчёты клиентов, а отработанные методологии».

**Когда появятся реальные клиенты**: вернуть звёзды и имена в блок отзывов, убрать «до» у цифр, переписать лид на странице сценариев. Не усиливать доказательства раньше, чем появятся настоящие.

## Где сейчас остановились

Ничего не сломано в коде и сборке. Миграция на Astro доведена до конца, слита в `main` и уже
в проде (`nevarium-lab.ru` отдаёт настоящий Astro-HTML — проверено `curl` на этой сессии).
Поверх неё на `main` легли ещё 3 новые услуги, фикс сетки 2×5, фикс пропусков этих услуг
в трёх местах, фикс чат-бота (кнопка «В начало» + ложные срабатывания на «бот») и ссылка на
VK. Working tree чист, последний коммит `main` — `19c246a` (2026-08-05 21:20 MSK).

**Единственная активная проблема** — она же самое важное в этом документе — расписана
отдельным блоком в самом верху: **CRM (`crm-nevarium.ru`) до сих пор не отвечает, а сайт
уже в проде**, значит реальные заявки с живого сайта прямо сейчас не доходят и посетитель
видит честную ошибку вместо успеха. Проверено на этой сессии.

## План на следующий шаг

1. **Первым делом** — проверить, не поднялась ли CRM: `curl https://crm-nevarium.ru` или
   сессия в `G:\nevarium-crm`. Пока не отвечает — это открытая проблема на живом сайте,
   не отложенная на будущее задача.
2. Если CRM всё ещё недоступна — стоит уточнить у пользователя: это ожидаемое временное
   состояние (сайт осознанно пущен в прод раньше CRM) или недосмотр, который надо поднять
   как срочный? В этой сессии решение о пуше без готовой CRM не задокументировано.
3. Самый ценный следующий шаг по существу, не технический: **собрать 2–3 реальных кейса с цифрами и согласием на публикацию**. Это по-прежнему единственное, что реально ограничивает убедительность сайта.
4. **Выбор постоянного хостинга** отложен пользователем на будущее — не поднимать самостоятельно.
5. Из `docs/MIGRATION_PLAN.md`, раздел 8 «Что делать после миграции»: разобрать `global.css` на части, убрать inline-стили из разметки, настроить автотесты и CI, вернуться к CSP (сейчас отложен из-за инлайн-скрипта темы).

## Грабли, на которые наступали — не повторять

1. **Никогда не писать слово «бесплатно» / «free» нигде на сайте.** Пользователь явно сказал: «I don't do anything for free». Это не разовая правка одной кнопки, а принципиальная позиция — проверять новый контент на этот счёт всегда.
2. **Перед изменением текста/формулировок — предлагать вариант с коротким обоснованием и спрашивать подтверждение**, а не сразу применять и не заваливать пользователя списком из 4 опций на выбор. Пользователь ценит одну обдуманную рекомендацию с "почему". Исключение — очевидные технические баг-фиксы (например, восстановление случайно удалённого текста, или технические находки при миграции вроде смены `client:visible` на `client:load`) можно делать сразу, но объяснять постфактум.
3. **Не пушить в GitHub без явной просьбы, и тем более не сливать ветки в `main` без явного подтверждения.** Пуш ветки для preview-деплоя — можно по запросу; слияние в `main` — отдельная, более весомая просьба.
4. **Пуш в прод при недоступной CRM создаёт на живом сайте честную ошибку вместо успеха отправки заявки** — это и произошло: `main` уже в проде, а `crm-nevarium.ru` по-прежнему не отвечает (см. блок в самом верху документа). Не «чинить» это самостоятельно подменой поведения (фальшивый экран успеха) — так и задумано, чинить нужно саму CRM. Раз это уже случилось на практике, а не только теоретический риск — при следующем пуше в `main` эту проверку стоит проговаривать вслух с пользователем явно, а не считать самоочевидной.
5. **SprintHost по FTP не работает** (ошибка авторизации, не разбирались дальше) — если пользователь снова заговорит о SprintHost, потребуется другой подход, либо явно уточнить, что учётные данные могли измениться.
6. **После любой визуальной правки — проверять и тёмную, И светлую тему, и мобильную версию.** На этом поймали не один баг. Пользователь также прямо просил «Check the mobile version first, too» перед коммитом — держать это в привычке по умолчанию.
7. **Browser preview pane иногда не композитит кадры**, если сама панель не отображается визуально — это ломает скриншоты, `requestAnimationFrame` (счётчики цифр) и `IntersectionObserver` (в т.ч. `client:visible`-гидратацию React-островов в Astro). Чинится через `preview_stop` + `preview_start` заново, либо просто игнорируется как известный артефакт среды — не путать с реальным багом сайта. Именно это несоответствие и навело на находку про `client:visible` для форм с ПДн (см. п.12 выше) — сама находка осталась актуальной независимо от причины.
8. **Временные npm-пакеты и скрипты для одноразовых задач** (например, `sharp` для нарезки логотипа или генерации og-image.png) — ставить/писать, использовать, затем `npm uninstall <pkg>` / удалить файл скрипта, проверить `git status`, что лишнего не осталось.
9. **Контент менять через файлы в `src/data/*`** (content.tsx, bot.ts, icons.tsx), а не через разметку внутри компонентов страниц — так проще пользователю ориентироваться, и это его явное пожелание (он не программист).
10. **Пользователь не разбирается в коде** — объяснять простым языком, без жаргона, показывать результат в браузере (скриншоты), не грузить архитектурными деталями без необходимости.
11. **При переносе разметки на Astro легко потерять пробел** на границе текста и тега (`text{' '}<a>` в JSX схлопывается в пробел, в Astro-шаблоне — нет) — проверять собранный HTML на такие места, если явный `{' '}` был в оригинале.
12. **`icon: <IconBot />` в данных (content.tsx) Astro не отрисует** — выведет `[object Object]`. Хранить нужно сам компонент (`icon: IconBot`), а не готовый элемент.
